int sum(int x, int y) {
  int sum = 0;
  sum = x + y;
  return sum;
}

int min(int x, int y){
  int sum = 0;
  sum = x - y;
  return sum;
}

int mul(int x, int y) {
  int sum = 0;
  sum = x * y;
  return sum;
}

float dev (float x, float y) {
  float sum = 0;
  sum = x / y;
  return sum;
}
